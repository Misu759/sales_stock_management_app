<div class="container">
  <div class="row justify-content-center">
    <div class="col-10">
      <%= render 'shared/errors', obj: @purchase %>
      <%= form_with(model: @purchase, class: "p-3 mb-3 rounded", local: true) do |f| %>
        <div class="form-group row">
          <%= f.label :ingredient_name, class: "col-2 col-form-label" %>
          <div class="col-10">
            <%= f.collection_select :ingredient_id, Ingredient.all, :id, :name, { prompt: t('defaults.prompt') }, class: "form-control rounded" %>
          </div>
        </div>

        <div class="form-group row">
          <%= f.label :amount, class: "col-2 col-form-label" %>
          <div class="col-10">
            <%= f.number_field :amount, class: "form-control rounded", placeholder: true %>
          </div>
        </div>

        <div class="form-group row">
          <%= f.label :purchase_date, class: "col-2 col-form-label" %>
          <div class="col-10">
            <%= f.date_field :purchase_date, class: "form-control rounded", value: Date.today %>
          </div>
        </div>

        <div class="form-group row">
          <%= f.label :arrival_date, class: "col-2 col-form-label" %>
          <div class="col-10">
            <%= f.date_field :arrival_date, class: "form-control rounded", value: Date.today %>
          </div>
        </div>

        <div class="form-group row">
          <%= f.label :waste_date, class: "col-2 col-form-label" %>
          <div class="col-10">
            <%= f.date_field :waste_date, class: "form-control rounded" %>
          </div>
        </div>

        <div class="form-group row justify-content-center">
          <%= f.submit class: "btn btn-outline-dark btn-lg" %>
        </div>
      <% end %>

      <div class="mb-3 text-center">
        <%= link_to t('defaults.back'), purchases_path, class: "text-info" %>
      </div>
    </div>
  </div>
</div>